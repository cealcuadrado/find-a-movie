import { FilterCrewPipe } from './filter-crew.pipe';

describe('FilterCrewPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCrewPipe();
    expect(pipe).toBeTruthy();
  });
});
