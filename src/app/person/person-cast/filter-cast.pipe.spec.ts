import { FilterCastPipe } from './filter-cast.pipe';

describe('FilterCastPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCastPipe();
    expect(pipe).toBeTruthy();
  });
});
